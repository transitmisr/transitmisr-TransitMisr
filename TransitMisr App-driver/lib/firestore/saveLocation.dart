

import 'package:cloud_firestore/cloud_firestore.dart';
import 'package:location/location.dart';

Future<void> saveDriverLocation({LocationData locData,String uID})async{


 /* Firestore.instance.collection('Drivers').document(
    'uID'
  )

   .setData({
    'location':locData
  });
*/
    //  .setData({ 'title': 'title', 'author': 'author' });
}